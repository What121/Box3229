<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bestom.huanyu"
    android:sharedUserId="android.uid.system">

    <!-- 关机权限 -->
    <uses-permission android:name="android.permission.SHUTDOWN" />
    <uses-permission android:name="android.permission.DEVICE_POWER "/>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 允许程序安装应用 -->
    <uses-permission android:name="android.permission.INSTALL_PACKAGES" />
    <!-- 允许程序删除应用 -->
    <uses-permission android:name="android.permission.DELETE_PACKAGES" />
    <!-- 允许应用清除应用缓存 -->
    <uses-permission android:name="android.permission.CLEAR_APP_CACHE" />
    <!-- 允许应用清除应用的用户数据 -->
    <uses-permission android:name="android.permission.CLEAR_APP_USER_DATA" />

    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.FilechoseActivity" />
        <activity android:name=".activity.ApklistActivity" />
        <activity android:name=".activity.ApkManagerActivity" />
        <activity android:name=".activity.PowerActivity" />
        <activity android:name=".activity.GpioActivity" />
        <activity android:name=".activity.KeyActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MyActivity" />
        <activity android:name=".activity.NetcfgActivity"></activity>
    </application>

</manifest>